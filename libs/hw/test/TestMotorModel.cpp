#include <gtest/gtest.h>
#include <thread>
#include <chrono>

#include "MotorModel.h"
#include "HwConfig.h"

TEST(HwTest, TestMotorModel) {
  // hw::MotorModel motor;

  // std::cout << "Ticks: " << motor.GetTicks() << std::endl;
  // motor.SetWheelSpeedRpm(1000);
  // std::this_thread::sleep_for(std::chrono::seconds(1));
  // std::cout << "Ticks after sec: " << motor.GetTicks() << std::endl;
}
